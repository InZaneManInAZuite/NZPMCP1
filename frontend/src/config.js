//import dotenv from 'dotenv'

// Configuration if Development
const config = {
    API: 'http://localhost:3001/api'  
}

/*
// Configuration if Production
if (process.env.NODE_ENV === 'production') {
    config.API = process.env.PORT
}
*/


export default config